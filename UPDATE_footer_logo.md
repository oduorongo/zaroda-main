# ZARODA SMS — Footer logo updated

Copy the whole folder over your working copy, rebuild the frontend.

## Change
The landing-page **footer** still used the old gold "Z" box with just "ZARODA". It now shows the
logo image with "ZARODA SCHOOL" (white) over "MANAGEMENT SYSTEM" (light orange) — matching the
header, sidebar, login and signup. No gold-Z placeholders remain anywhere.

## After deploying
Rebuild the frontend (Remove-Item -Recurse -Force .next; npm run dev), hard-refresh (Ctrl+Shift+R).
